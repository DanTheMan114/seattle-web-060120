<%= form_for @brews_customer do |f| %>
  Customer: <%= f.collection_select(:customer_id, @customers, :id, :customer_name ) %>
  Brew: <%= f.collection_select(:brew_id, @brews, :id, :blend_name ) %>
  <%= f.submit %>
<% end %>
<!-- collection_select(:post, 
  :author_id, Author.all, 
  :id, 
  :name_with_initial, 
  prompt: true) -->